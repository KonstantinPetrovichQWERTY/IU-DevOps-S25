apiVersion: v1
kind: Service
metadata:
  name: devops-info-service
  labels:
    app: devops-info-app
spec:
  type: NodePort
  selector:
    app: devops-info-app
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000
      nodePort: 30080
