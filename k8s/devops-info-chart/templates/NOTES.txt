1. Get the application URL by running these commands:
  {{- if eq .Values.service.type "NodePort" }}
  export NODE_PORT=$(kubectl get --namespace {{ .Release.Namespace }} -o jsonpath="{.spec.ports[0].nodePort}" services {{ include "devops-info-chart.fullname" . }})
  export NODE_IP=$(minikube ip)
  echo http://$NODE_IP:$NODE_PORT
  {{- else }}
  kubectl port-forward service/{{ include "devops-info-chart.fullname" . }} 8080:{{ .Values.service.port }}
  echo http://127.0.0.1:8080/
  {{- end }}
